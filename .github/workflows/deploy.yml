name: Deploy to evo.cl (Namecheap FTP — DISABLED, superseded by Netlify)

# Disabled 2026-06-30: evo.cl deploy moved to Netlify (Astro + Image CDN),
# triggered automatically by Netlify's own GitHub integration on push to main.
# This workflow is kept for reference / rollback only. Trigger manually if needed.
on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        with:
          server: premium345.web-hosti